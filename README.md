# DupHIST

⚠️ Documentation is currently under active revision.

This repository corresponds to **DupHIST v1.1.0**.
The README and full usage guide are being updated.

For basic information:

- Run `duphist --help` to see available command-line options.
- Refer to the `example/` directory for sample input structure.

## Installation (Conda)

Install via Bioconda:

```bash
conda create -n duphist
conda activate duphist
conda install -c bioconda duphist

